package git

// Note: Worktree operations in the main codebase are mostly done via bash commands
// embedded in tmux pane creation. This package provides helper functions for the few
// direct worktree operations.

// No additional worktree helpers are needed at this time as the operations
// are primarily done through bash command strings passed to tmux panes.
